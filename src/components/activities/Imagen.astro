---
export interface Props {
  src: string;
  alt: string;
}

const { src, alt } = Astro.props;
---

<div
  class="flex justify-center my-6 overflow-hidden rounded-lg border-2 border-gray-400 shadow-lg"
>
  <img src={src} alt={alt} class="max-w-full h-auto rounded-lg" />
</div>
